apiVersion: v1
kind: Service
metadata:
  name: travel-api-service
  namespace: travel

spec:
  type: ClusterIP

  selector:
    app: travel-api

  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
